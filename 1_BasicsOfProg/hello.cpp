#include <iostream>   // file containing implementations like cout etc.
using namespace std;  // defines the scope of the identifiers.
int main()
{
    cout<<"Hello world!"<<endl;  // single inverted commas for a single char and double inverted commas for string.
    return 0;
}
// a namespace is a collection of related names or identifiers (functions, class, Variables) which helps to separate these
// identifiers from similar identifiers in other namespaces or the global namespace.